# Required for the WebSocket Connection header in nginx.
# Drop this file at /etc/nginx/conf.d/connection-upgrade.conf on the nginx VM.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
